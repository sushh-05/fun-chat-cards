# AI Fun Chat Cards (Demo)

Small React + Vite demo that generates short playful cards using the OpenAI Chat Completions endpoint.

## Quick start (local demo)

1. `cd ai-fun-chat-cards`
2. `npm install`
3. Create `.env` (next to package.json) and add:

Use a throwaway/test key if publishing publicly.

4. `npm run dev`
5. Open http://localhost:5173

## Notes
- This demo uses a client-side API key (fast to demo). For production, move the key to a serverless function.
